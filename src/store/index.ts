// Deprecated barrel — superseded by the single data barrel at @/data (packet heal-2-01/heal-2-02).
// Kept only as a thin alias so any stale @/store import still resolves; do NOT add logic here.
export * from "@/data";
